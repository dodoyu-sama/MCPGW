# build artifacts (anchored to repo root — a bare `mcp-arc` would also match cmd/mcp-arc/)
/mcp-arc

# runtime / data dirs (docker volume mount, local sqlite audit db)
data/

# audit database
*.db
*.db-journal

# local config overrides
config.local.yaml

# node / web
web/node_modules/
web/dist/

# os
.DS_Store
